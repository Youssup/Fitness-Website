{
  "dependencies": {
    "vite": "^5.1.4",
    "express": "^4.18.2"
  },
  "scripts": {
    "build": "npm i --production && cd client && npm i && npm run build",
    "dev": "nodemon server/index.js --watch server/"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}